[project]
name = "multiprocessing-blockchain-simulation"
version = "1.0.0"
description = "An educational Python blockchain and proof-of-work simulation"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
